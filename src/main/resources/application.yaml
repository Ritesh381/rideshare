server:
  port: 8080

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: accuwu381@gmail.com
    password: ygrkwixxheoeygjq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
  kafka:
    bootstrap-servers: localhost:9092

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

    consumer:
      group-id: rideshare-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

  application:
    name: rideshare

  mongodb:
    uri: mongodb://localhost:27017/rideshare_db

jwt:
  secret: mysupersecretkey123456789mytokenkey
  expiration: 86400000 # 24 hours

app:
  name: RideShare Backend
  version: 1.0.0
  description: Ride sharing API

  data:
    redis:
      host: localhost
      port: 6379

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
